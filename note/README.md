This `note` directory is a bunch of "context pills" for claude.  The may
be useful for humans as well, but it's just an easy way to organize
a collection of "facts" without needing to turn them into a proper
document structure.
